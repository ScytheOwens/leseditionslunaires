<article class="widget-presentation">
    <img src="{{ medium }}" alt="Couverture de {{ title }}" class="widget-presentation-medium"/>
    <section class="widget-presentation-content">
        <h1 class="widget-presentation-content-title">{{ title }}</h1>
        <ul class="widget-presentation-content-details">
            {% for detail in details %}
                <li class="widget-presentation-content-details-item"><a href="{{ detail.link }}" class="link link-highlight">{{ detail.name }}</a></li>
            {% endfor %}
        </ul>
        <h2 class="widget-presentation-content-subtitle">Ajouter au panier :</h2>
        <ul class="widget-presentation-content-options">
            {% for option in options %}
                <li class="widget-presentation-content-options-item"><a href="{{ option.link }}" class="button button-highlight"><span>{{ option.name }}</span><span>{{ option.price }}</span></a>
            {% endfor %}
        </ul>
        <p class="widget-presentation-content-text">{{ description }}</p>
    </section>
</article>
